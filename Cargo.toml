[package]
name = "lobrust"
version = "0.1.0"
edition = "2021"
description = "Lobster movie streamer re-written in rust"

[dependencies]
clap = {version="4.5.4",features = [ "derive" ] }
reqwest = { version = "0.11", features = ["blocking", "json"] }
regex = "1.5"
mockito = "1.5.0"
